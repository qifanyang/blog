---
title: 正则表达式
date: 2016-05-03 14:27:32
tags: java
---

## 记不住的正则表达式
正则表达式语法比较抽象,都是一些符号,所以不好记,每次使用都要查询api来慢慢写和调试,其实正则表达式包含了很多知识,比如DFA(有穷状态自动机)

### 语言定义
“语言”就是指字符串的集合，其中的字符来自于一个有限的字符集合。也就是说，语言总要定义在一个有限的字符集上，但是语言本身可以既可以是有穷集合，也可以是无穷集合(不停的造新单词)

### 表示方法
1.ε 长度为零的集合
2.对字符集中任意字符a，表达式**a**表示仅有一个字符a的语言，即{ a }

### 正则逻辑运算
1.X|Y 并集
2.XY 连接,就是组合 x={a,b},y={c,d} , xy={ac,ad,bc,bd}
3.克林闭包 X* 表示将0个,1个,2个...无穷个x与自己连接 X* = ε | X | XX | XXX | XXX | ……

### 运算优先级
克林闭包>连接>并集

### 应用
1.string 就是字符连接运算s|t|r|i|n|g

2.变量名定义规则  字母开头紧接着零个或多个数字或者字母  
2.1 首字母正则定义为 a|b|c|d|e...太长了, 简写[a-z]
2.2 紧接着零个或多个数字或者字母正则定义 [a-z0-9]*
2.3 连接到一起 [a-z][a-z0-9]* 一个变量定义正则表达式就好了
2.4 [a-z]这些都是方便书写,类似助记符

### 正则表达式基础
只要两种要素和三种基本运算,可以组合出各种正则表达式

### 正则表达式辅助语法
1.方括号表示括号内的字符并运算。[abc]就等于a|b|c
2.方括号中以^字符开头，表示字符集中，排除方括号中的所有字符之后，所剩字符的并运算。[^ab]就表示除了ab以外所有字符求并。
3.圆.点表示字符集内所有字符的并。因此 .* 这个表达式就能表示这种字符集所能组成的一切字符串。
4.X?表示 X|ε 。表示X与空字符串之间可选。
5.X+表示XX*。这等于限制了X至少要重复1次。

### 正则表达式用于词法分析


### 有限自动机(finite automation),有限状态机(finite state machine)
正则表达式没法直接用于解析字符串,需要引进一种模型用于编程实现正则的功能. 正则的定义人可以直接理解,为了让机器可以理解,类似面向对象编程,需要抽象出一些属性和定义操作让机器完成特定的功能.

有限状态机:
1.包含有限[状态]集合
2.包含一个状态到另一个状态的[转换]
3.一个初始状态至少一个接受状态

有根从外边来的箭头指向的状态表示初始状态，有个黑圈的状态是接受状态：

![确定性有穷自动机](/img/dfa.png)

![非确定性有穷自动机](/img/nfa.png)

一个自动机接受的所有字符串组成的集合称作这个自动机的语言。这里语言的概念和上一回我们介绍正则表达式的语言概念是一样，都表示一个有限字符集上的字符串集合。

DFA、NFA和正则表达式是等价的，任何NFA都存在一个与之接受同样语言的DFA，和一个定义相同语言的正则表达式；同理任何正则表达式，也存在一个接受其所定义语言的NFA和一个DFA。这三种模型虽然定义迥然不同，但却表示同样的正则语言
 
转换: 正则表达式-->NFA-->DFA  , java正则直接使用NFA





http://www.cnblogs.com/Ninputer/archive/2011/06/08/2075714.html



